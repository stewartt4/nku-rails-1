<%= form_for @user do |f| %>
  <% if @user.errors.any? %>
  <div id="error_explanation">
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <ul><%= msg %></ul>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <small>
      <strong>Full</strong> <%= f.label :name %>:
    <%= f.text_field :name %>
    </small>
  </p>

  <p>
    <small>
    <%= f.label :nickname %>:
    <%= f.text_field :nickname %>
    </small>
  </p>
 
  <p>
    <small>
    <%= f.label :email %>:
    <%= f.text_field :email %>
    </small>
  </p>

  <p>
    <small>
    <%= f.label :image %>:<br>
      <strong>URL:</strong> <%= f.text_field :image %>
    </small>
  </p>
 
  <p>
    <%= f.submit class: 'btn btn-large btn-primary' %>
  </p>
<% end %>